spring:
  application:
    name: backend

  # ?????? ?? (PostgreSQL)
  datasource:
    url: jdbc:postgresql://localhost:5432/memoryseed
    username: ms-user
    password: memoryseeddb
    driver-class-name: org.postgresql.Driver

  # JPA / Hibernate ??
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: Asia/Seoul

  # OAuth2 ??? ??? ??
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 35707e5e434ed1b62b5720eb8e9c4cfd  # ??? ??????? ??? REST API ?
            client-secret: PCmESJAekA99GJBl5UycV0jo8YlcWaxg # ??? ??????? ??? Client Secret
            client-name: Kakao
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/kakao"
            client-authentication-method: client_secret_post
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

# ?? API (AccuWeather) ??
accuweather:
  base-url: https://dataservice.accuweather.com
  api-key: zpka_9225b9589f404a60b664d39031889e66_a7317a3b