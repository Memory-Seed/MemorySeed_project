server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/memorysedd
    username: memory
    password: memorypw
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
